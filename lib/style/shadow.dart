import 'package:flutter/material.dart';

import '../gen/colors.gen.dart';

class MeShadow {
  static const box = [BoxShadow(blurRadius: 1, offset: Offset.zero, spreadRadius: 0, color: ColorName.shadow)];
  static const snackBar = [BoxShadow(blurRadius: 10, offset: Offset.zero, spreadRadius: 0, color: ColorName.shadow)];
}
